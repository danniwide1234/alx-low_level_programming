#include "main.h"
#include <stdio.h>

/**
 * main - starting point to begin the instruction to print name
 *
 * @argc: argument count
 * @argv: argc vector
 *
 * Return: 0 (sucess)
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
