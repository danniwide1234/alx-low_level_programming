#include "main.h"

/**
 * _abs - works on absolute value of an integer
 *
 * @n: integer number to be worked upon
 *
 * Return: value of the integer in absolute form
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
