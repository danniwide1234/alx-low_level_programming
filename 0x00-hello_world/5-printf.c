#include <stdio.h>

/**
 * main - Entry point of C program that use the printf function
 * to display information.
 *
 * Return: 0 (Sucess).
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
