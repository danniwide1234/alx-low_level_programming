#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @p: The integer to be evaluated.
 *
 * Return: The absolute value of `p`.
 */
int _abs(int p)
{
	return ((p < 0) ? -p : p);
}
